# تاریخچه انتشار — نرم‌افزار دفتر خروج کالا

## نسخه ۱.۳.۰ — نمودارها، هوشمندسازی و انقضای گواهی

### 🎨 بازطراحی ظاهر (۱۱ مورد)
- **فونت فارسی وزیرمتن** باندل‌شده در EXE — همه متن‌ها یکدست و خواناتر
- **اسپلش‌اسکرین** هنگام باز شدن — دیگر چند ثانیه اول «هیچ» دیده نمی‌شود
- **جداکننده هزارگان فارسی** — «۳٬۵۰۰٬۰۰۰» به‌جای «۳۵۰۰۰۰۰» در جداول و هشدارها
- **پیام موفقیت شناور (Toast)** — «ذخیره شد ✓» گوشه صفحه محو می‌شود؛ دیگر برای هر پیام کلیک لازم نیست
- **حالت خالی جداول** — پیام دوستانه + راهنمای شروع وقتی جدول خالی است
- **نشان رنگی وضعیت بدهی** در جدول جستجو (قرمز = بدهی، سبز = تسویه‌شده)
- **حالت تیره** — سوییچ در تنظیمات ← تب عمومی؛ فوری اعمال می‌شود و در اجرای بعدی حفظ می‌شود
- **میان‌برهای صفحه‌کلید** — F1 تا F9 برای همه صفحات + Esc بازگشت به داشبورد
  (راهنمای میان‌برها: دکمه «⌨ میان‌برها» روی داشبورد)
- **یادآوری چیدمان** — سایز/موقعیت پنجره و صفحه‌ی آخرِ باز، در اجرای بعدی برمی‌گردد
- **ترنزیشن fade** هنگام تعویض صفحات — جابجایی نرم به‌جای پرش ناگهانی
- **ردیف اقدام سریع روی داشبورد** — ثبت مجوز، جستجو، تسویه بدهی، پشتیبان‌گیری با یک کلیک

### ✨ قابلیت‌های جدید
- **تب «نمودارها»** (PyQt6-Charts):
  - نمودار میله‌ای **خروج ماهانه** با ماه‌های شمسی
  - نمودار دایره‌ای **سهم شرکت‌ها** از کل خروج (با درصد)
  - نمودار خطی **روند شکل‌گیری بدهی‌ها**
  - فیلتر بازه‌ی تاریخ شمسی مشترک + خروجی **PNG** از هر نمودار
- **انقضای گواهی تولید** (مهاجرت ۳):
  - فیلد «تاریخ انقضا» (اختیاری) در فرم گواهی + اعتبارسنجی (انقضا ≥ صدور)
  - **گواهی منقض‌شده به‌عنوان گواهی فعال پذیرفته نمی‌شود**؛ خروج روی آن بدهی ثبت می‌شود (با تأیید صریح)
  - اولویت مصرف: گواهی با نزدیک‌ترین انقضا قبل از گواهی بدون انقضا مصرف می‌شود
  - هشدار داشبورد «گواهی‌های نزدیک به انقضا (۳۰ روز)» + نشان «⛔ منقض‌شده» و «⏳ نزدیک انقضا» در جدول گواهی‌ها
- **هوشمندسازی**:
  - **اتوکامپلیت نام کالا** از تاریخچه‌ی ثبت‌های قبلی (جستجوی حاوی-رشته)
  - **پیش‌بینی اتمام گواهی** بر اساس میانگین مصرف ماهانه: روی هشدار زرد داشبورد
    («حدود N ماه مانده — تا 1405/08»)
  - دکمه **«ثبت مشابه»** در صفحه جستجو: بازکردن فرم ثبت جدید با همان شرکت/کالاها/مقصد
    به‌عنوان الگو (شماره و تاریخ تازه، بدون اسکن‌های مجوز قبلی)
- **مانیفست روزانه PDF**: کل خروج‌های یک روز در یک سند رسمی با جمع‌ها
  (گزارش‌های رسمی → تب گزارش دوره‌ای → «مانیفست روزانه PDF»)
- **امنیت داده — پشتیبان دوم هفتگی**:
  - در تنظیمات، مسیر مقصد خارجی (فلش USB، درایو شبکه، دیسک دیگر) تعیین می‌شود
  - هفت‌روز یک‌بار هنگام باز شدن برنامه، آخرین پشتیبان ZIP به آن مسیر **کپی** می‌شود
    (محافظت در برابر خرابی دیسک/ویروس) — قاعده ۷ روز شمسی از آخرین کپی موفق
  - اگر مسیر مقصد در دسترس نباشد (فلش وصل نیست)، برنامه مختل نمی‌شود و فقط لاگ می‌شود
  - دکمه «اجرای پشتیبان دوم همین حالا» برای کپی فوری
- **امنیت داده — رمز ورود اختیاری**:
  - در تنظیمات ← تب عمومی ← گروه «امنیت»، رمز ورود تعیین/تغییر/حذف می‌شود
    (تغییر/حذف فقط با تأیید رمز فعلی؛ تکرار رمز جدید الزامی)
  - اگر رمز ثبت شده باشد، هنگام باز شدن برنامه دیالوگ ورود رمز نمایش داده می‌شود
    (حداکثر ۵ تلاش؛ رمز غلط → برنامه بسته می‌شود)
  - رمز هرگز به‌صورت خام ذخیره نمی‌شود: هش **PBKDF2-SHA256** با salt تصادفی
    و مقایسه‌ی زمان‌امن (compare_digest)
  - رمز اختیاری است — بدون ثبت رمز، برنامه مثل قبل باز می‌شود

### 🔧 بهبودهای فنی
- مهاجرت ۳: ستون `expiry_date` + ایندکس `idx_certificates_expiry` (idempotent و اتمیک مثل قبل)
- توابع سهمیه: `cert_expiry_status`، `is_cert_expired`، `is_expiring_soon`،
  `forecast_certificate_exhaustion`، `get_expiring_certificates`
- وابستگی جدید: `PyQt6-Charts` (در requirements و باندل EXE لحاظ شد؛ اگر نبود صفحه نمودار پیام مناسب می‌دهد)
- ایمن‌سازی چاپ کنسول در ویندوز (UTF-8 با جایگزینی) — اجرای برنامه از خط فرمان با codepage فارسی دیگر کرش نمی‌دهد

### 🐛 رفع اشکالات (رابط کاربری)
- **برش متن‌ها در لبه راست همه صفحه‌ها**: عنوان گروه‌ها (مثل «فیلترهای جستجو» و «نتایج»)
  در حالت RTL از کادر بیرون می‌زد و نصفه رندر می‌شد — اصلاح قاعده `QGroupBox::title`
  در استایل (رفتار معکوس left/right در RTL)
- **چسبیدن محتوا به لبه پنجره** در صفحه‌های جستجو، گزارش‌های رسمی، شرکت‌ها،
  بدهی‌ها و نمودارها — حاشیه بیرونی استاندارد اضافه شد
- **فیلتر بازه تاریخ نمودارها** فقط در تب آخر دیده می‌شد — فیلتر مشترک
  به بالای هر ۳ تب منتقل شد و همیشه قابل استفاده است
- **به‌هم‌ریختن صفحه تنظیمات در ماکسیمم**: بعد از افزودن گروه‌های پشتیبان دوم
  و امنیت، محتوای تب عمومی از ارتفاع پنجره بلندتر شده بود و در ماکسیمم ویجت‌ها
  روی هم می‌افتادند — کل محتوای تب داخل **QScrollArea** قرار گرفت (اسکرول عمودی
  در ارتفاع کم، چیدمان سالم در هر اندازه)
- استایل محلی تب تنظیمات که عنوان گروه‌ها را می‌شکست حذف شد (استایل سالم کلی اعمال می‌شود)
- تست خودکار برش متن (`tests/diag_clipping.py`): هر ۹ صفحه را پیکسل‌به‌پیکسل
  بررسی می‌کند که هیچ متن/فیلدی از کادر بیرون نزند — در حالت ماکسیمم و
  تب عمومی تنظیمات با اسکرول کامل

## نسخه ۱.۲.۰ — بهبودهای عملیاتی و تجربه کاربری

### ✨ قابلیت‌های جدید
- **صفحه «تسویه بدهی‌ها»**: لیست بدهی‌های تسویه‌نشده به تفکیک مجوز با انتخاب
  گواهی فعال هم‌واحد برای هر ردیف؛ تسویه گروهی در یک تراکنش اتمیک.
- **صورتحساب شرکت (PDF)**: گردش کامل شرکت — گواهی‌ها + مجوزها + بدهی‌ها —
  در یک سند رسمی (گزارش‌های رسمی → تب ریز مصرف).
- **چاپ مجدد روبرگه**: دکمه «چاپ روبرگه» در صفحه جستجو برای هر مجوز
  (چاپ مستقیم یا ذخیره PDF).
- **جستجو با شماره ثبت**: فیلتر جدید در صفحه جستجو («1405/012» یا فقط «012»).
- **سربرگ سازمانی**: عنوان سازمان و لوگو در تنظیمات؛ در روبرگه و همه‌ی
  گزارش‌های PDF چاپ می‌شود (لوگو به‌صورت data-URI جاسازی می‌شود).
- **نصاب ویندوزی**: اسکریپت Inno Setup + ساخت خودکار نصاب در `build.bat`
  (اگر Inno Setup 6 نصب باشد). حذف برنامه، داده‌های کاربر را حذف نمی‌کند.
- **دابل‌کلیک = ویرایش** در جداول جستجو و شرکت‌ها + میان‌بر **Ctrl+S**
  برای ذخیره مجوز.
- **ورود اعداد فارسی**: فیلد مقدار کالا ۱۲٫۵ و ۱۲.5 را می‌پذیرد.
- **کارت‌های داشبورد قابل کلیک**: میان‌بر به صفحه مربوط (بدهی → تسویه بدهی‌ها).

### 🔧 بهبودهای فنی
- **مهاجرت ۲**: قید UNIQUE نام شرکت و (شرکت، شماره گواهی) در سطح دیتابیس +
  حالت **WAL** برای همروندی بهتر (پایه‌ی چند-کاربره شبکه‌ای).
- **گارد تک‌نمونه**: اجرای هم‌زمان دو نسخه روی یک دیتابیس مسدود می‌شود
  (حفاظت شمارنده شماره مجوز).
- **لاگ فایل سراسری**: `data/app.log` چرخشی (۵×۲MB) + شکار خطاهای ناگرفته
  (sys/threading excepthook) + هدایت پیام‌های Qt؛ دکمه «مشاهده لاگ خطاها»
  در تنظیمات.
- **نگهداری پشتیبان‌ها**: تنظیم تعداد نگهداری (پیش‌فرض ۳۰)؛ پاک‌سازی خودکار
  بعد از هر پشتیبان روزانه.
- **رفع N+1**: کالاهای نتایج جستجو با یک کوئری دسته‌ای بارگذاری می‌شوند.
- timeout اتصال SQLite به ۱۵ ثانیه افزایش یافت (سناریوی شبکه).
- راهنمای استقرار و اجرای شبکه‌ای: `docs/DEPLOYMENT.md`.

## نسخه ۱.۱.۰ — مکانیزم مهاجرت دیتابیس و پایداری داده

### ✨ قابلیت‌های جدید
- **مهاجرت نسخه‌دار دیتابیس** (`database/migrations.py`):
  - شماره نسخه ساختار در `PRAGMA user_version` نگهداری می‌شود.
  - در اولین اجرای هر نسخه جدید، روی سیستم‌های نصب‌شده به‌صورت خودکار مهاجرت اعمال می‌شود.
  - پیش از هر مهاجرت، پشتیبان خودکار در `backups/pre_migration/` گرفته می‌شود (۱۰ نسخه آخر نگه داشته می‌شود).
  - هر مهاجرت اتمیک است؛ در صورت خطا rollback می‌شود و `user_version` هم برنمی‌گردد.
  - گارد downgrade: اگر دیتابیس از برنامه جدیدتر باشد، مهاجرت رد می‌شود تا داده آسیب نبیند.
  - رویدادها در `data/migrations.log` ثبت می‌شود؛ خطای مهاجرت با دیالوگ واضح نمایش داده می‌شود (نه کرش).
- **پنل «اطلاعات نسخه» در تنظیمات**: نمایش نسخه ساختار دیتابیس، نسخه نرم‌افزار و تاریخ آخرین مهاجرت.
- **دکمه «خروج از بایگانی»** برای گواهی‌های بایگانی‌شده (برگرداندن به فعال/تمام‌شده).
- آیکون برنامه (`logo.ico`) حالا داخل باندل EXE است — آیکون تسک‌بار روی سیستم مشتری همیشه درست است.

### 🐛 رفع اشکالات
- **تسویه بدهی گواهی**: ثبت گواهی جدیدِ تسویه‌کننده حالا `certificate_id` واقعی را روی ردیف‌های بدهی ثبت می‌کند (تراکنش اتمیک). پیش از این، ویرایش/حذف مجوزِ بدهیِ تسویه‌شده ممکن بود سهمیه را به گواهی اشتباه برگرداند.
- **بایگانی گواهی بدهی‌دار** ممنوع شد (پیام با نمایش مقدار بدهی) — دیگر گواهی بدهیِ تسویه‌نشده قفل بایگانی نمی‌شود.
- **فیلتر «فقط خروج‌های بدهی»** در جستجو حالا فقط بدهی‌های **تسویه‌نشده** را نشان می‌دهد — هم‌خوان با آمار داشبورد.
- **بازگردانی پشتیبان قدیمی**: بعد از restore، مهاجرت‌ها بلافاصله اجرا می‌شوند تا ساختار دیتابیس هم‌تراز برنامه بماند.
- **شماره صفحه اسکن‌ها در ویرایش مجوز** حالا از شماره‌های موجود ادامه می‌یابد (پایدار در برابر حذف/جابجایی).
- ذخیره تنظیمات به UPSERT تبدیل شد — روی نصب‌های قدیمیِ بدون seed کلید، دیگر بی‌صدا شکست نمی‌خورد.

### 🔧 بهبودهای فنی
- ۷ ایندکس سرعتی روی جداول پرکاربرد (مهاجرت ۱).
- smoke-test سراسری (`tests/smoke_app.py`)، تست مهاجرت (`tests/test_migrations.py`) و تست UI تنظیمات.
- تست چرخه کامل بدهی (`tests/test_debt_cycle.py`).

### 📦 استقرار
فایل `dist/DafterKhorojKala.exe` را روی سیستم مقصد جایگزین کنید. در اولین اجرا:
1. مهاجرت‌های در انتظار به‌صورت خودکار (با پشتیبان قبلی) اعمال می‌شوند.
2. پشتیبان‌های پیش از مهاجرت در `backups/pre_migration/` کنار EXE نگهداری می‌شوند.

## نسخه ۱.۰.۰ — انتشار اولیه
- ثبت مجوز خروج کالا با شماره‌گذاری خودکار سالانه
- مدیریت شرکت‌ها و گواهی‌های تولید با کنترل سهمیه و بدهی
- اسکن WIA و افزودن تصویر نامه، مشاهده‌گر تصاویر
- چاپ روبرگه «خروج بلامانع» و PDF، گزارش دوره‌ای PDF، ریز مصرف گواهی
- جستجوی پیشرفته و خروجی Excel
- پشتیبان‌گیری خودکار روزانه و بازگردانی ZIP (ضد Zip Slip)
- تقویم شمسی گرافیکی و اعتبارسنجی تاریخ واقعی
